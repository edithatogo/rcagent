# Context Pack: Fail-closed primary pre-launch gate

Track `eval-blocker-remediation_20260803`, issue #1. Prepared after read-only
slot binding on `codex/prospective-slot-session`. Reconcile its exact merged
head, tree, validation and ownership before activation. No primary adapter or
usable execution permit exists merely because this context is prepared.

## Gap, reuse and bounded inputs

The binder connects a selected native request to point-in-time filesystem
eligibility, but leaves the primary adapter unverified. The legacy freeze helper
checks committed bytes for a legacy component list; it is not a native execution
permit. A callable primary lifecycle must not precede its pre-launch gate.

Read the selected track specification/plan/metadata/cursor, workflow/guidelines,
standing decisions, slot binder and native/prospective protocol, existing freeze,
fixed server session and directly relevant tests. Limit context to this dependency
chain; do not read historical raw outputs or run a model. Use existing validators
and bounded Git lookups rather than inventing a second provenance system.

## Design and acceptance checkpoint

Main owns integration and evidence; assign bounded implementation and read-only
acceptance agents before changes. First specify the missing primary adapter and
complete component-closure requirements, then implement a read-only pre-launch
verifier with positive/adversarial synthetic contract tests. It must explicitly
reject an absent adapter or incomplete closure. A fabricated supplied receipt,
legacy `freeze_verified` status or caller boolean cannot bypass missing evidence.
Do not label a partial component list complete or substitute READY/binder identity.

The intended gate checks exact native protocol/slot, fresh internal eligibility,
an explicit commit and actual complete dependency identities, plus hash-bound
agent-review evidence under existing standing authority. Define how that evidence
is obtained and verified before accepting it; a schema-valid claim is insufficient.
Keep execution permission separate from admitting completed observations and from
scoring-start. Avoid circular self-hash/commit requirements: gate and adapter source
can share a frozen commit while an external reviewed record refers to that commit.

If the complete adapter-dependent verifier cannot yet be implemented honestly,
deliver only the independently testable prerequisite checks with explicit pending
gates. Do not ship an always-pass placeholder or claim full gate completion from
an always-fail stub. Synthetic fixtures must not issue a usable production permit.
The later primary adapter's sole entry must verify all prerequisites before any
receipt reservation, temporary directory, worker, socket or process is created.
Preserve the fixed READY entrypoint and its historical source-pin receipts.

## Validation, boundaries and handoff

Require fixture-first tests, meaningful branch coverage, native/Windows types,
full repository validation and agent-panel review; retain negative results.
No actual freeze, model/cache acquisition, model run, READY retry, study admission,
blinding or scoring in this slice. Preserve private-data, credentials, source rights,
Apache-2.0, historical H0–H8/H8P and all existing raw evidence. Agent review grants
no clinical, legal, policy, regulatory, employment, cultural-safety, organisational
or deployment approval. No repeated owner approval for this bounded engineering.

Record exact implemented versus pending checks, deliver only reviewed green
changes and verify merge tree parity. Roll back only new verifier/prerequisite
code. Next: gated primary lifecycle, complete freeze verification, two-slot
orchestration and affirmative observation admission before blinding.
