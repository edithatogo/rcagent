# Context Pack: Protocol-bound single-slot session adapter

Track `eval-blocker-remediation_20260803`, issue #1. Prepared on
`codex/prospective-slot-session` after native protocol PR #97 merged as
`09a9058`, tree `76c7d6da6bf44d5be675bb8ae3acdfb099c785e3` matching reviewed
head `a36dcef`. Post-merge conformance `33321812017` and Quality `33321812059`
passed. Reconcile fresh workspace/ownership before activation. This is a
prepared recommendation, not implementation or primary execution evidence.

## Smallest next gap and inputs

The pure runner and native protocol validator construct slot-keyed requests but
do not bind them to an execution lifecycle. Build a fixture-only single-slot
adapter before a two-slot orchestrator. Read the selected track records,
workflow/guidelines, continuation cursor, native validator, pure runner, existing
fixed server session and its directly imported eligibility, process, transport
and decoding contracts. Load only those files and matching tests; no historical
raw outputs or unrelated tracks.

## Ownership and design checkpoint

Main owns integration and records; assign implementation and read-only acceptance
agents before changes. Determine exact source/test ownership during activation.
Select a request only from validated native protocol bytes and exact denominator
membership. Compare declared condition identities with fresh fixed-profile/model
eligibility, without treating declaration consistency as attested identity.

Reuse lifecycle and Unix transport controls through a narrow internal extraction
if needed. Preserve the READY entrypoint's fixed prompt, arguments, deadlines,
eligibility checks, receipt format and behavior. No arbitrary callback, arbitrary
request escape hatch, TCP fallback or bypass flag. Retain protocol, slot, request,
response and cleanup provenance. Do not introduce a callable primary-execution
path before full-component freeze and affirmative admission controls exist.
If that boundary cannot be maintained by the proposed extraction, implement only
the pure request/identity binding contract and record remaining lifecycle work.

## Tests, exclusions and handoff

Exercise exact slot selection, declaration/reference/identity mismatches, changed
bytes and synthetic lifecycle composition. Use fixture-first tests, meaningful
coverage, native/Windows types, full repository validation and agent-panel review.
Fixtures cannot count as study slots. No actual model run, READY retry, freeze,
scoring, blinding, primary admission or new acquisition is authorised by this
context. Preserve all negative observations, historical evidence and source-pin
snapshots. Apache-2.0 and per-artefact rights remain unchanged.

Agent engineering review is not clinical, legal, policy, regulatory, employment,
cultural-safety, organisational or deployment approval. Record exact local and
hosted outcomes, verify merge tree parity and save the next dependency-ordered
cursor. Rollback only this adapter/extraction, preserving existing READY behavior.
Later gates: two-slot orchestration, complete transitive freeze and affirmative
admission-before-blinding. No repeated owner approval for bounded fixture work.
