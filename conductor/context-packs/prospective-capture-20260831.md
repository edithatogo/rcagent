# Context Pack: Frozen prospective two-slot capture

## Terminal outcome — do not invoke again

The sole invocation completed at 2026-08-30T21:37:06Z with
`controller_failed`: first slot `incomplete_generation` at the fixed 512-token
limit, second slot not-attempted, 0/2 admitted. Three-agent negative-evidence
review passed. [Actual receipt](../tracks/eval-blocker-remediation_20260803/prospective-capture-20260831.md)
supersedes the historical activation/in-progress notes below. No retry, new
root, altered cap, blinding or scoring. Deliver this result, then use Branch B
of the [post-capture context](./prospective-postcapture-20260831.md).

Prepared successor, not an execution receipt. Activate only after the exact
source/review freeze preflight, three-role review, full validation, hosted
delivery and post-merge checks are verified. Start at AGENTS and the Conductor
index, then the owning plan/cursor, standing decisions 20260830-001/002 and
the [freeze receipt](../tracks/eval-blocker-remediation_20260803/prospective-execution-freeze-20260831.md).

Ready checkpoint: PR #102 merged normally as `cecf385`, matching reviewed tree
`16a12305dacbc8a0d4677d22513cab35d9c322ed`; all seven checks and both post-merge
workflows passed. Fresh fetch verified S/R ancestry. Parent branch cleanup and
local master were verified; `codex/prospective-two-slot-capture` is prepared.
Activate the capture plan task before invoking the controller. No study-evidence
root, capture journal, primary observation or score exists from this slice.

Activated from clean checkpoint `cf9e71d` on the prepared branch after fresh
PR #102/post-merge verification and S/R ancestry/source parity checks. The
canonical evidence root was absent. Main owns the sole controller invocation;
agent lanes are read-only operational-evidence reviewers, not scorers. Baseline
full validation is running before invocation. Preserve an in-progress invocation
across client/session boundaries; never restart it or infer failure from silence.

Activation commit `3d1693a`; baseline passed 1,536 tests in 290.23 seconds at
94.56%. Main created the sole canonical root with mode 0700 and invoked the
controller once using `.venv/bin/python -I`, explicit repository import path,
fixed protocol/R arguments and exclusive mode-0600 return/stderr files. No
source bytes changed. Invocation is in progress; operational results and panel
review remain pending. The queue retains the observed process/session cursor.

## Exact bounded scope

Use protocol `evaluation/prospective/prospective-agent-text-20260830/protocol.json`
at SHA-256 `650186fd54dd832532ea995c954862026203b848bb520e8e0cae7a4af8c80628`,
source S `e6c1453d68b562829b99f40b925de6d287496601` and review R
`cd09dba47704f3c87b95975a216a9a5be98158bd`. Verify both commits remain ancestors
of the delivered branch and all execution bytes still match; any source change
requires a new reviewed freeze before execution. Do not repin evidence to pass.

The existing approved cache is `/Volumes/PortableSSD/rcagent-model-cache`.
The canonical evidence root for this study is its new `study-evidence` child,
created once with mode 0700. Check exact path, ownership, symlinks and existing
run state before creation/use. Never select another root to evade consumed
attempts; preserve any existing journal and raw receipts without overwrite.
No acquisition, new provider, credential, global runtime change or spend.

Before inference, disclose the pinned Qwen2.5 0.5B Instruct Q4_K_M model,
llama.cpp server 0.3.0 profile, Darwin/arm64 device, exact source/model pins,
public synthetic cases, seed 42, temperature 0, 512 output-token cap, 2048
context setting and 120-second session deadline. The fixed native completion
request has no chat-template wrapping. Byte checks do not establish token fit;
runtime truncation/failure remains evidence, not grounds to alter the prompt.
Only a private Unix socket is permitted; OS/egress isolation is not attested.
No clinical, legal, policy, employment, cultural-safety or operational use.

## Run and verify

Main owns this one integration lane. Use a fresh isolated interpreter with
the same explicit repository path and environment identity as the reviewed
preflight. Invoke only `prospective_study_controller.run_study` once for both
ordered slots; do not call the primary entry per slot or repeat old probes.
The controller rechecks eligibility and review gates before attempts, retains
exclusive journals and raw receipts, and performs immediate admission. Do not
skip checks to reduce hashing time. No automatic retry or resume follows a
failure, interruption, timeout, consumed attempt or partial receipt.

Keep generated raw output and local evidence private. Inspect exact journal,
slot receipts, cleanup and returned admission state against the pinned
denominator; distinguish failed, unknown and not-attempted dispositions.
Record hashes and a reviewed minimal public projection, not raw model output.
Have the panel review observed evidence and claims; votes cannot cure missing
receipts or turn a failed slot into success. Report negatives unchanged.

## Subsequent work and fallback

Capture success does not unlock blinding or scoring. Saved admission JSON
cannot recreate the live capture witness. Next implement the separately
reviewed trusted custody/scoring transition binding exact raw/journal/admission
bytes, S/R and frozen inputs. Use fresh scorer contexts, three sealed original
submissions and a separate adjudicator only after that gate passes.

Keep historical H0–H8/H8P and planning inventory separate. A failed condition
requires preserved evidence and bounded diagnosis, not a new attempt disguised
as recovery. Apply the approved readiness-only fallback only on evidenced
ineligibility, not unfinished engineering. Root and owning-track acceptance
remain broader than this study. Pause only at genuinely new authority or an
unavailable path with no independent ready work; do not re-request routine
review, execution already within the approved envelope, or approved fallback.
