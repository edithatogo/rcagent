# Context Pack: Explicit llama 0.3 runtime profile

Track: eval-blocker-remediation_20260803; issue #1. Base `4582b8e`, merged
PR #84; branch `codex/runtime-profile-v030`. Created 2026-08-30, fresh until
any pinned source, binary, licence or profile byte changes. Load only selected
track records, comparator decision 20260829-002, decisions 20260830-001/002,
existing runtime helper/probe tests and exact local package evidence.

## Scope and fit

Reuse the existing pinned Darwin verifier and bounded receipt capture through
an explicit version-module parameter; keep its no-argument legacy behaviour,
constants and digest stable. Add a separate fixed llama 0.3 profile and CLI.
No arbitrary caller-supplied manifest, executable or extra flags through CLI.
This is a thin profile extension, not a dependency resolver or new runtime.

Main owns `tools/darwin_runtime_v030.py`, its fixed byte inventory and track
records. The implementation agent owns shared-helper changes plus its focused
tests. A separate agent reviews local rights/provenance without writes.
No overlapping file writers or worktree isolation lease are configured.

## Acceptance and boundaries

Check exact installed executable, libraries, alias targets, backend inventory,
licences and provenance evidence. Clear the child environment, verify actual
loader reports, and retain complete bounded local version/help diagnostics.
Pin both the version module and shared helper before/after. Preserve missing
old-profile files and all historical receipts; never silently update old pins.
Use only already installed/cached bytes; no download, installation, global link
change, credentials, model inference, private data or redistribution here.

The installed libomp header SPDX and licence agree on Apache-2.0 with LLVM
Exceptions, unlike its formula/SBOM MIT summary. Preserve that discrepancy.
Likewise the new llama SBOM's old dependency labels do not override installed
receipt/file/loader evidence. Classification supports bounded local diagnostics,
not legal validation, distribution or study admission.

## Tests and handoff

Require fixture-first compatibility, cross-profile rejection, source/pin drift,
exclusive receipts, full diagnostics and no-inherited-secrets tests. Run focused
pytest with isolated coverage if concurrent, Ruff/types, full validation and
agent review before live diagnostic execution. Record current loader findings;
do not claim model suitability. Next: fixed synthetic output-mode probe and
runner/normalisation, then actual protocol freeze and affirmative admission.
Rollback the explicit extension while preserving old evidence and study locks.
