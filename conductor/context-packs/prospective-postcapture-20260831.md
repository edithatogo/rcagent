# Context Pack: Prospective post-capture custody or negative evidence

Prepared conditional successor for `eval-blocker-remediation_20260803`, root
issue #1. This document records no capture outcome and activates no task.
Selection update: actual capture failed at the fixed output cap with 0/2
admitted. Three-role negative-evidence review passed; select Branch B after
delivery of the [actual receipt](../tracks/eval-blocker-remediation_20260803/prospective-capture-20260831.md).
Branch A is inapplicable to this consumed run. No positive custody engineering
is needed for its unadmitted response. First reconcile existing inventory and
root acceptance before proposing any new verifier.
Activate only after the current capture outcome, exact evidence, agent review
and delivery have been verified. Start at AGENTS and `conductor/index.md`, then
the owning plan/cursor, standing decisions 20260830-001/002, the
[capture context](./prospective-capture-20260831.md) and its actual delivery
receipt. Do not infer success from a file, checkbox or positive JSON field.

## Frozen scope and ownership

Preserve source S `e6c1453d68b562829b99f40b925de6d287496601`, review R
`cd09dba47704f3c87b95975a216a9a5be98158bd` and protocol SHA-256
`650186fd54dd832532ea995c954862026203b848bb520e8e0cae7a4af8c80628`.
The canonical private evidence root is
`/Volumes/PortableSSD/rcagent-model-cache/study-evidence`. Never change roots,
overwrite evidence, resume a partial run or repeat a consumed slot. Retain both
expected slots and their actual dispositions. Preserve historical H0–H8/H8P,
planning snapshots and prior negative probe evidence.

Main owns integration, trusted outcome recording and delivery. Assign one
bounded implementation lane and separate acceptance and safety/evidence review
lanes after checking ownership. New engineering belongs outside the frozen
execution source list: proposed `tools/prospective_scoring_custody.py` and
direct tests. Existing execution modules may be imported read-only; do not add
reverse imports or edit their bytes to accommodate custody. Historical
`evaluation_preflight` and Track 5/6 entrypoints remain unchanged and fail-closed.

## Branch A: verified immediate admission

Use this branch only if the actual controller invocation and readbacks establish
both captures and successful immediate admission, with no conflicting failure
outcome. A saved `admission.json` is evidence, not a rehydratable capability.

Implement the smallest separate custody and scorer-packet preparation entry.
Its trust root is the existing owner-controlled reviewed repository workflow,
not a new key service or a self-declared receipt. Main records actual invocation
and panel findings; tests and fabricated reviewer envelopes cannot supply that
provenance. Select an exact trusted custody commit C through that workflow,
using a fixed record path such as `conductor/reviews/prospective-custody.json`.

Before C, commit and review the new custody implementation and its complete
supported source closure. C binds that earlier source identity, its review
evidence, the original S/R and environment identities, protocol, canonical run
identity, actual invocation/outcome evidence locator and hash, and exact sizes
and hashes of journal, admission and both raw receipts. Keep private evidence
local; committed records contain only a reviewed minimal projection. Preserve
S/R ancestry. Do not embed C's own hash in the bytes that determine C or treat
arbitrary caller-supplied commit/hash combinations as independent authority.
Document same-user, interpreter-memory and reviewer-authenticity limitations.

The entry must verify the trusted C record and reviewed custody source closure,
then independently check bounded owned-file reads, original evidence hashes,
the sealed journal chain and exact two-slot denominator, request reconstruction,
native response decoding, original capture/cleanup/provenance consistency and
absence of conflicting failure evidence. Reject fixtures, duplicates, missing
slots, replaced files and premature transitions. Never call the private live
witness issuer/consumer to recreate admission. Validating historical evidence
must not launch a model or require a new eligibility scan of the current cache.

Only after all checks may it exclusively create private scorer packets and a
separate custody mapping. All three scorer contexts receive the same exact
case, unchanged decoded response, frozen rubric and scoring-instruction bytes,
under opaque response identifiers. Withhold model/runtime/condition labels,
diagnostics and peer ratings; retain hashes and ordering evidence. Do not edit
self-identifying response text: disclose metadata-only blinding limitations.
Fail closed on existing output, partial persistence or identity drift; preserve
evidence instead of silently replacing it.

An affirmative result permits scoring only for the exact bound packet set.
Packet preparation does not mean agents have scored, submissions are sealed,
unblinding is authorised or analysis is complete. Actual scoring remains a
subsequent explicit workflow transition using fresh contexts, three sealed
original submissions and a separate adjudicator under the frozen instructions.

Use fixture-first positive composition and adversarial tests: untrusted or
altered C, wrong source/protocol, copied fixture evidence, conflicting failure
outcome, broken journal, missing/duplicate slots, altered request/response,
symlink/FIFO/hardlink/replacement, persistence faults and no-overwrite handling.
Prove failed gates create no packets and invoke no model or cache admission.
Run repository validation and agent review; synthetic tests never constitute
actual custody or primary observations. Deliver the bounded implementation
before applying it to actual private evidence.

## Branch B: failed, interrupted or unadmitted outcome

Do not create a positive custody/scoring record. Retain the original private
run directory and all available raw, journal, failure and controller evidence.
Publish only a panel-reviewed minimal projection of hashes, exact dispositions,
technical failure stage and limitations. Preserve unknown versus known failure
and not-attempted status; a START entry alone does not prove model execution.

Diagnose narrowly from retained bytes and pinned source, without model launch,
retry, prompt changes, new condition selection or alternate evidence roots.
Any independently useful engineering may receive a separate bounded context,
but cannot rewrite the frozen execution history or turn fixtures into results.
An unavailable or incomplete admitted cohort in this frozen study does not
prove that every possible model/runtime is unavailable. Apply the approved
readiness-only fallback only with evidence for its stated scope, no performance
or agreement results, and remaining engineering distinguished from eligibility.
Root/track closure still requires their broader acceptance maps.

## Authority and handoff

Routine implementation, agent-panel review, rework and approved fallback do not
need another owner approval. No acquisition, provider call, new credentials,
spend, global runtime change, raw-output publication or additional study attempt
is authorised by this context. Preserve Apache-2.0 and per-artefact rights;
allow no private clinical or employee data into public projections or scorer
packets. Agent agreement is not human agreement or clinical, legal, policy,
regulatory, employment, cultural-safety, organisational or deployment approval.
Escalate only genuinely new scope or reserved authority, once per stable issue.

Record the selected branch and exact verified outcome before activation. End
each delivered slice with implemented checks, observed evidence, unresolved
limits and the next dependency-ready action separately identified.
