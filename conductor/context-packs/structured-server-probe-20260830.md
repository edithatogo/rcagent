# Context Pack: Structured non-study server observation

Track `eval-blocker-remediation_20260803`, issue #1. Base `64810aa` (PR #92),
tree `b128fd7ac5912c29b27ce690122f2cd8beff7ba3` equals reviewed head `2176465`.
Branch `codex/structured-server-probe`; no lease enabled. Main owns execution,
local receipt inspection and public projection; acceptance and implementation
agents provide read-only review. This is not a primary study run.

## Purpose and authority

Observe the actual native response/transport/runtime composition once, rather
than repeat version/help or CLI output diagnostics. Reuse the delivered fixed
`prospective_server_session.capture_session` unchanged. Standing decisions
20260829-002 and 20260830-002 cover bounded local synthetic comparator work;
fresh per-artefact eligibility is an evidence gate, not another owner approval.
Both reviewing agents recommend a bounded attempt with the checks below.

Read the selected plan/spec/metadata, workflow/guidelines, prior session receipt,
fixed session/model/profile contracts and the two standing decisions. Do not
load unrelated tracks, historical raw outputs or private data.

## Preconditions and boundaries

Require exact-head green delivery and source bytes matching the merged commit
before importing in a fresh Python process. Use only the canonical pre-existing
`/Volumes/PortableSSD/rcagent-model-cache`, with full original class/licence
eligibility and the separate fixed server profile checked before and after.
Reserve a new exclusive private receipt under its existing `probe-evidence`
directory. Check no other session owns this work; no reused receipt or overwrite.

Use the unchanged synthetic READY prompt, fixed request/arguments/environment,
private Unix socket, bounded capture and explicit stop/join/reap checks. No model
download, credential, new network route, arbitrary options, private content,
redistribution, global installation or study case. Preserve all historical pins.

## Observation and projection

Require successful return plus receipt readback/hash. Independently inspect
complete transport, native decoding/EOS, full loader logs with matching child
PID, unchanged source/model/profile identity and verified cleanup. Record exact
decoded content equality with `READY` separately: never strip or case-fold.
All admission flags remain false. Source hashes are observations, not a frozen
study protocol or loaded-code/OS attestation.

Keep raw receipt/logs/content local. Public projection includes only reviewed
commit/source/profile/model/request/receipt/content hashes, byte counts,
outcome/error, transport/EOS/cleanup checks, exact-match boolean and limitations.
Session success proves only this bounded observation, not model quality,
peer authentication, clinical validity or study/historical completion.

## Recovery, validation and handoff

Preserve every failure and inspect before retry. At most two evidence-led fixes;
no silent pin, decoder or threshold relaxation. Worker/reaping uncertainty is a
hard stop for replacement launches. Execution deadline does not bound preflight
hashing or OS process creation; do not claim containment. If live work is blocked,
continue independent runner/freeze fixtures rather than fabricate admission.

Validate changed records and any actual implementation delta, run panel review,
record exact receipt hashes and delivery state, then advance the continuation
cursor. No new approval is required for routine in-scope rework. Rollback removes
only successor code/records; preserve raw negative observations and parent code.
