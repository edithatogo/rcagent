# Track 00 review and remediation

## Scope

Fresh conductor-review against specification, plan and source at base
`02e7245dcccfad3d9e44fa5c2af340373ae1941a`, using separate acceptance,
correctness/safety and evidence-integrity agent lanes. Shared context and
correlated errors limit independence; exact model revisions are unavailable.
This is repository engineering review, not clinical or organisational approval.
The existing Apache-2.0 choice and private-data restrictions are unchanged.

## Findings and disposition

| Finding | Disposition |
|---|---|
| Empty or malformed compliance matrices and missing evidence could pass | Require the reviewed requirement IDs, typed fields and readable in-repository evidence |
| An incomplete profile could produce current conformance | Require a complete profile for a positive current-conformance receipt |
| An arbitrary successful executable was labelled the official validator | Use this interpreter's isolated official module and verify installed VCS provenance against the reviewed source pin; custom commands remain unverified diagnostics |
| Symlinked resources could escape the portable package | Reject linked package roots/resources before reading contents, including Windows-style escape paths |
| Upstream comparison could omit renamed or truncated normative changes | Reject malformed, divergent or potentially capped comparisons and inspect both rename paths |
| Claude actual activation and representative execution evidence is absent | Block acceptance criterion 9 and archival; do not substitute installer or contract tests |

Installation provenance checks identify the declared official repository,
subdirectory and commit. They are not signed attestations or protection against
an attacker who can rewrite the local environment and metadata.

## Acceptance boundary

The retained Codex held-out and output-quality trials are bounded evidence for
their recorded client/version, prompts and observations. They do not establish
universal trigger safety or current behaviour of every client. The historical
Phase 7 receipt's generalisation beyond its three trials per case is superseded
by this explicit limitation; its original bytes and negative runs are retained.

The specification requires each adapter to pass activation and representative
execution tests. Claude's retained receipt establishes deterministic contract
and installation behaviour, not those actual observations. The missing
requirement is now visible as `RCA-ADAPTER-001` instead of being omitted from
the compliance matrix. No agent vote, successful fixture or green unrelated
test may fill that evidence gap. A bounded synthetic-only Claude trial may
proceed only with applicable client/account execution authority; no credential
change, private input or new spending authority is inferred here.

The initial live upstream check on 2026-08-31 resolved the reviewed revision
`69ef37e9424c0a7ea9dd2293b559e43ec8176379`. That is upstream revision evidence,
not proof that the incomplete full track passes conformance or may be archived.
The [fresh full-conformance receipt](./conformance-review-20260831.json) records
the official validator passing with verified installation provenance and a
matching live upstream revision at 12:25 UTC. Full conformance exits 1 solely
because `RCA-ADAPTER-001` remains pending; `current_conformance` is false.

Code repair commit: `036137295959674a45e4a49356702d0ad03c7cb9`.
Focused regression across the four changed validator modules plus the
server-stop regression suite passed **140 tests**. Ruff passed. The safety
panel's follow-up found an additional offline baseline-error exit-code gap;
it was fixed and covered in that run. The first broad run overlapped active
profile edits and ended with 1537 passes and three failures (two superseded
profile assertions and a server-stop reap timing assertion). It is retained
as an unsuccessful run, not a baseline certification.

The fresh `.venv/bin/python -m tools.full_validation` run ended with **1622
passed, 2 failed**, 94.65% coverage, on Darwin/Python 3.14.5. Ruff, ty,
basedpyright, governance, gremlin and benchmark checks passed before pytest.
The remaining failures are `test_timeout_terminates_and_reaps` and
`test_ignored_term_fails_and_is_sent_once`: both expected `reaped=true` after
a 0.1-second cleanup grace but observed false. The server-process source was
unchanged in this review. Timing sensitivity is not proof of an environmental
cause; these failures remain unresolved and the broad gate is not green.
The final offline-error regression was added after full-suite collection and
is covered by the separate 140-test focused run. No shared focused coverage
result substitutes for the broad gate. Hosted checks were queued when first
inspected; local checks are not hosted-CI success.

The three agent lanes reviewed acceptance/profile, portable safety/runner,
and archive/evidence integrity. Applicable Markdown guidance was followed;
no applicable manifest-selected platform guide was present. Agent agreement
does not establish the missing actual-client observations.

## Archive gate and recovery

Do not move this track to the archive until the actual-client requirement and
all applicable review/validation gates pass. When eligible, update executable
track locators, the registry, roadmap, integration map, dependency links,
documentation and current matrix paths together. Preserve the hash-bound
historical distribution dependency manifest and existing evidence bytes.

This review uses a separate temporary clone because the original checkout is
unavailable. It neither recovers nor certifies preservation of original
local-only evidence. No historical study retry or model-cache access occurred.
Rollback only the reviewed fixes and current status reconciliation; retain
this finding and historical observations rather than claiming false completion.
