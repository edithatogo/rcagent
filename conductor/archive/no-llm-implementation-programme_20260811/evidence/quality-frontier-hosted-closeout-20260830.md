# Quality-frontier hosted closeout

Observed at: `2026-08-30T02:56:33Z`
Data boundary: public repository metadata and repository-owned configuration

## Ruleset

GitHub API responses for ruleset `21834601` and the effective rules on
`refs/heads/master` agree:

- enforcement is active;
- deletion and non-fast-forward updates are blocked;
- pull requests require zero approving reviews and no CODEOWNERS, last-push or
  review-thread gate;
- the repository owner retains an always-on recovery bypass; and
- strict required checks are `dependency-review`, `validate (ubuntu-latest)`,
  `validate (macos-latest)`, and `validate (windows-latest)`.

The durable hosted settings URL is
<https://github.com/edithatogo/rcagent/rules/21834601>.

## Renovate

The Renovate GitHub App created Dependency Dashboard #74 at
`2026-08-30T02:44:48Z`:
<https://github.com/edithatogo/rcagent/issues/74>.

The Dashboard detected Git submodules, GitHub Actions, PEP 621 dependencies,
and the inherited Renovate configuration. Proposed updates are categorised as
pending approval or awaiting schedule. No update pull request was created or
approved at this checkpoint, and the repository catch-all continues to set
`automerge` to `false`.

## Disposition

Issues #17 and #18 have direct repository and hosted evidence. They may close
after this exact evidence change passes required checks and merges. Root issue
#1 remains open pending a separate root acceptance audit and evidence-backed
disposition of every root criterion and active legacy track.

The legacy evaluation study remains preserved outside this programme. Its
unfinished repository-owned work includes AU public-source collection, case
formatting and QA, manifest re-audits, a positive admission fixture, Gemini
trials, and final client conformance. Its external work includes accountable
appointments, credentials, provider and human execution, scoring authority,
and protocol disposition. These states are not waived or completed here.

This receipt is repository engineering evidence only. It is not clinical,
legal, policy, regulatory, employment, cultural-safety, organisational or
deployment validation or approval.
